// examples/demo.ltl — runnable demo for ltl_8080_emu

import std::io
use ltl_8080_emu as lib

fn main() {
    io::println("ltl_8080_emu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}